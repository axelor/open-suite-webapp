spotless {
  java {
    target fileTree(projectDir) {
      include '**/*.java'
    }
    googleJavaFormat()
  }
}

project.afterEvaluate {
  tasks.spotlessJava.dependsOn(tasks.generateCode)
}

task formatCode(dependsOn: ['licenseFormat', 'spotlessApply'])

build.dependsOn 'spotlessApply'
